# Versioned assets archive

這裡收納已不再被 `index.html` 入口引用的舊版快取檔。

目前正式入口保留在專案根目錄：

- `script.js`
- `script-v17.js`
- `styles.css`
- `styles-v17.css`
- `styles-dark.css`
- `styles-dark-v17.css`

注意：

- `舊的/**` 已被 Firebase Hosting 排除，不會部署。
- 若要回查舊版行為，優先用 Git history；這裡只保留曾經產生過的版本化靜態檔。
