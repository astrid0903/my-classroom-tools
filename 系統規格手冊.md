# my-classroom-tools 系統規格手冊

## 1. 專案概述
本專案為 **我的班級工具總專案**，主要包含多功能簡報展示與講師學員即時互動的小工具（Google Slides 教學播放台、貼文板與座標獵人等遊戲）。
本工具採用無伺服器架構（Serverless Web App），主要由 HTML、JavaScript 與 Vanilla CSS 構成，並透過 Firebase Firestore 與 Storage 提供後端資料同步與圖片儲存。

## 2. 系統架構與技術棧
* **前端**：Vanilla HTML5, CSS3 (搭配 CSS Variables 與 media overrides 實現高質感暗色/亮色主題與自訂貼文板背景), Vanilla JavaScript (ES6+)。
* **即時通訊**：Firebase Firestore SDK，提供講師端與參與者端雙向資料傳播。
* **資料儲存**：Firebase Cloud Storage (圖片儲存) 及 IndexedDB / localStorage (講師本地簡報及設定檔快取)。
* **部署平台**：GitHub Pages 與 Firebase Hosting 靜態代碼託管；Firebase Hosting 透過 `.firebaseignore` 與 `firebase.json` ignore 清單排除本機敏感檔與測試檔。

## 3. 核心功能規格
### 3.1 講師端（主頁面 index.html）
* **簡報播放台**：可播放 Google Slides 與 Google Drive 中的 PDF 檔案。
* **小工具系統**：支援多個獨立小工具浮動視窗，包含 Timer（計時器）、Clock（時鐘）、Groups（分組名單）、Text（純文字框）、Image（圖片顯示）、YouTube 播放器等。
* **貼文板（Post Board）**：
  * 可動態編輯區段、文字內容。
  * **版面與操作統一**：看板區段均採用橫向 flex 單列延伸（小螢幕或多區段時不折行），以產生橫向滾動條方式呈現；每個區段標題下方設有虛線 `+` 新增貼文按鈕，提供與學員端完全一致的操作體驗。
  * 支援背景顏色/自訂背景圖設定，且背景可無級調整透明度。
  * **配色對比算法與深色基礎**：貼文板預設為深色主題與暗色玻璃態卡片。當設定背景圖片時，半透明遮罩會改為深黑色（`rgba(10, 14, 20, veil)`）以自動將背景圖調暗並維持深色風格；只有在背景圖片不透明度為 100% 且極亮時，系統才會透過 JavaScript 的 Luminance 亮度算法，動態將卡片邊框與文字配色切換為亮底暗字（`.page-bg-light`）以確保高可讀性。
  * **QR Code 與短網址彈窗**：點擊放大 QR Code時，彈窗背景卡片在淺色背景下會自動切換為純白色背景，並於關閉按鈕上方顯示精簡的參與者投稿頁短網址，支援點擊 URL 或複製按鈕將其寫入剪貼簿。
  * **QR Code 小彈窗切換**：講師端右上角「QR Code」按鈕採明確開關邏輯，展開後再次點擊同一按鈕即可收合；小彈窗在暗色主題下使用不透明深色背景。
  * **貼文詳細檢視**：講師端一般貼文卡片可直接點擊展開詳細內容，與參與者端檢視 modal 共用同一套呈現邏輯；貼文內超連結、星號與選單按鈕會阻止誤觸展開。
  * **星號置頂按鈕**：講師端星號按鈕在暗色與亮底覆蓋狀態下均強制透明背景、無陰影，避免受全域 dark button 樣式影響出現方塊底。
  * **附件檔案（任意格式）**：貼文可附加 1 個任意格式的檔案，與圖片共用同一個檔案選擇欄位（**不設 `accept`**）。選檔後以 `isImageFile()` 分流：`image/*` 進圖片陣列並沿用壓縮流程，其餘一律成為附件。附件以 base64 data URL 原樣存進貼文文件本身（不使用 Cloud Storage、不轉檔、MIME 原樣保留），單檔上限 `POST_FILE_MAX_LENGTH = 900000` 字元（約 659 KB 原檔），且圖片與附件合計受 `POST_ATTACHMENT_MAX_TOTAL_LENGTH = 950000` 字元限制，以守住 Firestore 單筆文件 1 MB 上限。檢視時以 `dataUrlToBlob()` 還原成 Blob URL 開新分頁或下載（data: URL 無法直接做頂層導向）；膠囊徽章文字由 `attachmentBadgeLabel()` 取副檔名，配色由 CSS `[data-kind]` 決定。
  * **投票型貼文**：支援建立「投票型」貼文，可自訂最多 10 個選項，發布後即時在看板上以精美進度條顯示票數佔比與得票，資料隨學員投票即時同步更新。
* **播放台檔案存檔機制（本機檔／Google Drive 檔共用）**：
  * **自動存檔**：`scheduleFileAutoSave()` 以 900ms debounce 觸發 `autoSaveCurrentFile()`；`fileSaveInProgress` / `fileSaveQueued` 負責把連續編輯合併成一次寫入（中間的重複請求丟棄，只補跑最後一次）。
  * **存檔序列化**：所有存檔路徑——自動存檔 `performAutoSaveCurrentFile()` 與手動存檔 `performSaveCurrentFile()`——一律經 `withFileSaveLock()` 排進同一條 promise 隊伍，同時最多只有一個寫入在跑。任一次存檔失敗不會卡住隊伍（`fileSaveLock` 對成功與失敗都續接）。手動存檔進入隊伍前會 `clearTimeout(fileSaveTimerId)` 取消待觸發的自動存檔。
  * **Drive 衝突偵測**：`saveCurrentDriveFile()` 寫入前先 `getDriveFileMetadata()` 取雲端 `modifiedTime`，與本機 `currentDriveModifiedTime` 比對，雲端較新才跳覆蓋確認。**此檢查的正確性完全依賴「寫入後要記寫入後的時間戳」**——上傳回應缺 `modifiedTime` 時會重讀一次 metadata 補上，取不到則寫入空字串（停用檢查），**絕不可退回記寫入前的 `metadata.modifiedTime`**，否則之後每次存檔都會誤判衝突且無法自行恢復。
  * ⚠️ **反面教材（v1.6.2 前）**：手動存檔未經互斥鎖，與進行中的自動存檔交錯時，後跑者讀到前者剛寫入的 `modifiedTime`，對使用者呈現為莫名其妙的「Google Drive 上已有較新的版本」。新增任何會寫回檔案的入口時，務必走 `withFileSaveLock()`。

### 3.2 參與者/投稿端（由 URL 帶 `?board=boardId` 觸發模式切換）
* **強制姓名輸入與快取**：首次進入貼文板時強制彈窗要求輸入名字並快取至 `localStorage`，隨後新增貼文時自動填入。支援右上角一鍵點擊修改名字。
* 即時檢視貼文板區段與所有學員上傳的貼文。
* 新增貼文（含文字及多圖上傳，上限 5 張，另可附 1 個任意格式的檔案）。
* 編輯與刪除自己的貼文（受限於 authorUid 和 Firestore rules）。
* **即時投票互動**：支援對投票型貼文進行點擊投票（單選轉移，且可點擊已投選項進行取消）。
* **超連結配色一致性**：無論是在一般卡片中、公告看板、或是詳細內容彈窗中，在亮色背景下統一使用 `#2572b8` 藍色字體加底線（暗色主題亮色背景則為 `#2364aa`），在暗色背景下統一使用 `var(--d-cool)` (#5fa8ff) 亮藍色字體，保證與講師端的超連結視覺風格一致。

## 4. Firestore 資料庫綱要
### 4.1 `pages` 集合 (貼文板資料)
* `id` (string): 貼文板 ID/頁面 ID。
* `name` (string): 貼文板名稱。
* `type` (string): 固定為 `"posts"`。
* `boardId` (string): Firestore 資料庫唯一對應 ID。
* `sections` (array): 包含多個區段物件 `{ id, title, description }`。
* `participantMode` (string): `"edit"` (允許新增/編輯自己的貼文), `"add"` (僅允許新增貼文), `"view"` (僅唯讀)。
* `note` (string): 公告文字（HTML/純文字）。
* `background` (object): `{ type, color, image, opacity }`。

### 4.2 `posts` 集合 (個別貼文資料)
* `id` (string): 貼文 ID。
* `boardId` (string): 所屬的貼文板 ID。
* `sectionId` (string): 所屬的區段 ID（為空則屬未分類）。
* `author` (string): 作者暱稱/座號。
* `authorUid` (string): 投稿者的 Firebase 匿名登入 Uid。
* `content` (string): 貼文文字內容。
* `images` (array): 圖片的 Storage URL 陣列。
* `createdAt` (timestamp): 建立時間。
* `fileDataUrl` (string): 附件的 base64 data URL（任意 MIME，例如 `data:application/pdf;base64,...`、`data:application/zip;base64,...`），無附件時為空字串；rules 上限 900,000 字元，不限 MIME。
* `fileName` (string): 附件原始檔名（經 `sanitizeAttachmentName()` 去路徑、過長時保留副檔名截斷）；rules 上限 120 字元。
* `type` (string): 貼文類型（`"text"` 或 `"poll"`）。
* `pollOptions` (array): 投票選項陣列，例如 `["選項1", "選項2"]`。
* `pollVotes` (map): 投票數據。鍵為選項 index (string, 如 `"0"`, `"1"` 等)，值為投票學員的 Firebase `uid` 陣列。

## 5. 安全性與存取控制 (Firestore Rules)
* 系統採用 Firebase 匿名認證 (`signInAnonymously`) 作為身份識別。
* `pages` 文件僅允許擁有該頁面 ID 權限的端讀取與更新狀態。
* `posts` 文件的 `write` 規則受限：
  * **建立**：需為有效的已驗證用戶，且 `createdAt` 為當前時間。
  * **編輯與刪除**：必須驗證 `request.auth.uid == resource.data.authorUid`，防止非原創作者惡意修改他人貼文。
  * **內容非空判定**：`hasPostBody()` 接受文字、圖片、投票選項或**附件檔案**其中之一，因此「只附一個檔案、沒有文字」的貼文可合法建立。
  * **投票特別例外**：對於類型為 `poll` 的貼文，允許非作者的已驗證用戶在保證其餘欄位（如作者、內容等）完全與原 document 一致的前提下，僅更新 `pollVotes` 與 `updatedAt` 屬性，此時受到 `isPollVoteOnlyUpdate` 規則保護，防止篡改內容；該規則同時鎖定 `fileDataUrl` 與 `fileName` 必須與原文件一致，避免藉投票夾帶換檔。

## 6. 部署與本機敏感檔規格
* Firebase Hosting 的 `public` 目錄目前為專案根目錄，因此必須同時維護 `.firebaseignore` 與 `firebase.json` 的 `hosting.ignore` 清單。
* **附件格式不設限**：貼文附件不做 MIME 白名單，學員可上傳任意格式（含可執行檔）。檔案僅存於 Firestore 文件內、由下載者自行開啟；如需收斂風險，將貼文板參與模式改為 `view`。
* 不可部署或提交的本機/敏感檔包含：`client_secret*.json`、`.env`、`*.key`、`credentials.*`、`.firebase/**`、`.claude/**`、`.codex/**`、`scratch/**`、`播放台存檔/**`、`舊的/**`、`node_modules/**`。
* Google OAuth client secret JSON 僅允許存放於專案外本機私密路徑，例如 `/Users/hsiaoyutsai/.config/my-classroom-tools/client_secret.json`；repo、Hosting 目錄、文件與對話中都不得記錄 secret 內容。
